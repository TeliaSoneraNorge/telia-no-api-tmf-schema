definitions:
  LogicalResource:
    $ref: https://raw.githubusercontent.com/TeliaSoneraNorge/interface_definitions/master/tmforum/TMF639_ResourceInventory/19.5/TMF639-ResourceInventory-v4.0.0.swagger.json#/definitions/LogicalResource
  PortingInfo:
    portingDate:
      type: string
      format: date-time
    status:
      type: string
    error:
      type: string
    info:
      type: string
    transactionDate:
      type: string
      format: date-time
    from:
      type: string
    to:
      type: string

title: LogicalResourceTelia
type: object
allOf:
  - $ref: "#definitions/LogicalResource"
  - properties:
      portingInfo:
        type: array
        items:
          $ref: "#/definitions/PortingInfo"
